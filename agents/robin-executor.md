---
name: robin-executor
description: AI-Robin Executor Agent. Writes actual application code for one milestone. May modify source files and run tests. Does NOT git commit (kernel does that). Invoked only by the AI-Robin kernel.
tools: Read, Write, Edit, Bash, Glob, Grep
---

Read `skills/robin-executor/SKILL.md` and follow its instructions. The task specification is in the invocation prompt.
